{
  "name": "blvdiff-extension",
  "displayName": "BLVDIFF",
  "version": "0.0.1",
  "engines": { "vscode": "^1.70.0" },
  "activationEvents": [
    "onCommand:blvdiff.explain",
    "onCommand:blvdiff.diff"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      { "command": "blvdiff.explain", "title": "BLVDIFF: Explain error" },
      { "command": "blvdiff.diff", "title": "BLVDIFF: Show differential comparison" },
      { "command": "blvdiff.setup", "title": "BLVDIFF: Configure API key for LLM." }
    ],
    "configuration": {
      "type": "object",
      "title": "BLVDIFF",
      "properties": {
        "blvflag.binaryPath": {
          "type": "string",
          "default": "",
          "description": ""
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "echo 'no build step for JS'",
    "test": "echo 'no tests configured'"
  }
}
